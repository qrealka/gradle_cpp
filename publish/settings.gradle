/*
 * This settings file was auto generated by the Gradle buildInit task
 * by 'dloginov' at '11/11/15 4:16 PM' with Gradle 2.8
 *
 * The settings file is used to specify which projects to include in your build.
 * In a single project build this file can be empty or even removed.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user guide at https://docs.gradle.org/2.8/userguide/multi_project_builds.html
 */


// automatically include all directories under uploads/ as projects
// each subproject is atrtifact to upload to repo

rootProject.name = 'publish'
def uploadDir = file('uploads')
uploadDir.eachDir { include it.name }

rootProject.children.each {
    it.projectDir = new File(uploadDir, it.name)
    it.buildFileName = "${it.name}.gradle"
}
